services:
  ai-service:
    build: ./apps/ai-service
    ports:
      - "8000:8000"
    env_file:
      - ./apps/ai-service/.env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
