import Hero from "@/components/sections/hero";
import Scale from "@/components/sections/scale";

const Home = () => {
  return (
    <main>
      <Hero />
      <Scale />
    </main>
  );
};

export default Home;
